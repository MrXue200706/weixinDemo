技术点：
1.weui库
2.wxparse解析html富文本插件,支持解析html/md/emoji

notes:
1.改变data里的属性，要用page对象的setData()方法
2.回调函数防止不传入时报错，使用短路运算
3.导入样式文件要放在根目录导入
4.<image></image>是双标签
5.navigator页面导航的url不用写文件后缀，app.json里面也不用
6.stopPullDownRrfresh()取消下来刷新，一般完成请求后就可以取消下拉，提高体验
7.路由时地址栏传参：在url参数里?后面拼接参数,注意参数键值是用等于号连接id={{item.id}}